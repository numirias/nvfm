#!/bin/bash

HERE="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

mkdir -p /tmp/nvfm
export NVIM_RPLUGIN_MANIFEST=/tmp/nvfm/rplugin.vim
export NVFM_START_PATH=/home/nius/
export NVFM_PLUGIN_PATH=$HERE/../

nvim -u $HERE/../script/init.vim
